defaults:
  - _self_
  - model: deepconvlstm
  - dataset: har_dataset
  - strategy: clean

# Training Hyperparameters
epochs: 50
batch_size: 32
lr: 0.001
weight_decay: 0.0001
patience: 20
aux_weight: 0.4

# Experiment Settings
results_dir: "results"
seed: 42
limit_folds: null
resume_dir: null
