defaults:
  - _self_
  - model: mobilevit
  - dataset: har_dataset
  - strategy: robust

# Training Hyperparameters
epochs: 50
batch_size: 32
lr: 0.001
weight_decay: 0.0001
patience: 20
aux_weight: 0.4

# Experiment Settings
results_dir: "results"
seed: 42
limit_folds: null
resume_dir: results\mobilevit_robust_20251128_023824
